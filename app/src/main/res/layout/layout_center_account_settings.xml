<merge xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Creates the layout of Profile Menu(Account Settings) which is accessible from the top right
     corner of the user's profile page -->
    <RelativeLayout
        android:layout_height="wrap_content"
        android:layout_width="match_parent"
        android:padding="20dp"
        android:background="@color/white">

        <TextView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:id="@+id/textSettings"
            android:text="Account Settings"
            android:textSize="30dp"
            android:textColor="@color/black"
            android:background="@drawable/grey_border_bottom"/>

        <!-- Creates the list view containing the options to change user's account settings -->
        <ListView
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:layout_below="@+id/textSettings"
            android:id="@+id/lvAccountSettings">
        </ListView>

    </RelativeLayout>


</merge>